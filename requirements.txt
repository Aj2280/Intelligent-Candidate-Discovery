# Redrob Hackathon — Candidate Ranker
# Install: pip install -r requirements.txt

# Core ML
sentence-transformers>=2.7.0
torch>=2.5.0          # CPU version — install via:
                      # pip install torch --index-url https://download.pytorch.org/whl/cpu
numpy>=1.26.4

# Data handling
pandas>=2.2.2
tqdm==4.66.4
jsonlines==4.0.0

# Utilities
pyyaml==6.0.1
rich==13.7.1

# Sandbox / demo (HuggingFace Spaces)
gradio>=4.37.2
